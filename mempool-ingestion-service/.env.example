# Mempool Ingestion Service .env.example

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Mempool WebSocket URL (e.g., Alchemy, Infura, direct node)
# Replace with your actual WebSocket URL
MEMPOOL_WS_URL="wss://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY"

# Port for the publisher WebSocket server (for MevBotV10Orchestrator to connect to)
PUBLISHER_PORT=3001

# Optional: Firestore configuration if direct Firestore output is also used
# GCP_PROJECT_ID="your-gcp-project-id"
# FIRESTORE_COLLECTION="mempool_transactions_stream_v1"
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/gcp-service-account-key.json" # For local Firestore auth if not using emulator with ADC
